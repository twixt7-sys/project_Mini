const variable = "hello";

console.log(variable);

function HelloScreen() {
    return (
        <div>
            <h1>Hello, World!</h1>
            <p>This is a simple React component.</p>
        </div>
    );
}